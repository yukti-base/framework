ext {

    springBootVersion = "4.1.0"

    lombokVersion = "1.18.32"

    postgresVersion = "42.7.7"

    hibernateTypesVersion = "2.21.1"

    // Added for core module's JWT verification -- was previously hardcoded
    // separately inside auth-service's own build.gradle and copy-pasted into
    // admin-service's. Centralizing here means bumping the JJWT version is a
    // one-line change instead of an N-service hunt.
    jjwtVersion = "0.12.6"

}